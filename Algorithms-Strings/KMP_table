void KMP_table(string S, int *kmp) {
	int i, j = 0, N = S.length();
	kmp[0] = 0;
	for (i = 1; i < N; i++) {
		while (j > 0 && S[i] != S[j]) {
			j = kmp[j - 1];
		}
		if (S[i] == S[j]) {
			j++;
		}
		kmp[i] = j;
	}
}
